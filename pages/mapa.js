import MapComponent from "../components/MapComponent";

const Mapa = () => {
  return (
    <div className="Mapa">
      <h1 style={{display: "flex", justifyContent: "center"}}>Mapa de recursos</h1>
      <MapComponent />
    </div>
  )
}

export default Mapa;